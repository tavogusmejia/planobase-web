# dependencias
node_modules/
.pnpm-store/

# build
.next/
out/
build/
.turbo/
.vercel/
next-env.d.ts
*.tsbuildinfo

# entorno — nunca se versiona
.env
.env.local
.env.*.local

# material pesado, fuera del repo
assets-originales/
media/

# sistema
.DS_Store
*.log
.leads/

# estado local del CLI de Supabase — cambia por máquina
supabase/.temp/
.vercel
.env*

# Worktrees de Claude Code. Viven dentro del repo y tienen su propio git; que
# aparezcan como «sin seguimiento» en cada `git status` solo hace ruido.
.claude/worktrees/
